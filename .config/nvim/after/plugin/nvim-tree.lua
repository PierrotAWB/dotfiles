require('nvim-tree').setup {
    renderer = {
        icons = {
            show = {
                file = false,
                folder = true,
                folder_arrow = true,
                git = false
            }
        }
    }
}
